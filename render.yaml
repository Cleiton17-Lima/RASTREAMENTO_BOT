services:
  - type: web
    name: rastreamento_boot
    env: python
    region: brasil   # ou sua região preferida
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT

